<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>인생역전! 로또 번호 추천</title>
    <style>
      :root {
        --bg-color: #f8f9fa;
        --container-bg: #ffffff;
        --primary-color: #4a90e2;
        --primary-hover: #357abd;
        --text-color: #333;
        --title-color: #222;
        --subtitle-color: #888;
        --border-color: #eeeeee;
        --ball-shadow: rgba(0, 0, 0, 0.05);

        /* Lotto Ball Colors */
        --color-1-10: #fbc400; /* Yellow */
        --color-11-20: #69c8f2; /* Blue */
        --color-21-30: #ff7272; /* Red */
        --color-31-40: #aaaaaa; /* Gray */
        --color-41-45: #b0d840; /* Green */
      }

      body.dark-mode {
        --bg-color: #121212;
        --container-bg: #1e1e1e;
        --text-color: #e0e0e0;
        --title-color: #ffffff;
        --subtitle-color: #a0a0a0;
        --border-color: #2d2d2d;
        --ball-shadow: rgba(0, 0, 0, 0.3);
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family:
          'Pretendard',
          -apple-system,
          BlinkMacSystemFont,
          system-ui,
          Roboto,
          'Helvetica Neue',
          'Segoe UI',
          'Apple SD Gothic Neo',
          'Noto Sans KR',
          'Malgun Gothic',
          sans-serif;
      }

      body {
        background-color: var(--bg-color);
        color: var(--text-color);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 20px;
        transition:
          background-color 0.3s ease,
          color 0.3s ease;
      }

      .container {
        width: 100%;
        max-width: 500px;
        background: var(--container-bg);
        padding: 40px 20px;
        border-radius: 24px;
        box-shadow: 0 10px 30px var(--ball-shadow);
        text-align: center;
        position: relative;
        transition:
          background-color 0.3s ease,
          box-shadow 0.3s ease;
      }

      .theme-toggle-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        background: none;
        border: none;
        font-size: 1.4rem;
        cursor: pointer;
        padding: 8px;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        transition:
          background-color 0.3s ease,
          transform 0.2s ease;
        color: var(--text-color);
      }

      .theme-toggle-btn:hover {
        background-color: var(--border-color);
        transform: scale(1.1);
      }

      .theme-toggle-btn:active {
        transform: scale(0.9);
      }

      h1 {
        font-size: 1.8rem;
        margin-bottom: 30px;
        color: var(--title-color);
        transition: color 0.3s ease;
      }

      .ball-container {
        display: flex;
        justify-content: center;
        gap: 12px;
        margin-bottom: 40px;
        min-height: 60px;
        flex-wrap: wrap;
      }

      .ball {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        font-weight: bold;
        font-size: 1.2rem;
        color: white;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
        box-shadow:
          inset -4px -4px 8px rgba(0, 0, 0, 0.15),
          4px 4px 10px var(--ball-shadow);
        opacity: 0;
        transform: scale(0.5) translateY(20px);
        transition: box-shadow 0.3s ease;
      }

      .ball.show {
        animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
      }

      @keyframes popIn {
        0% {
          opacity: 0;
          transform: scale(0.5) translateY(20px);
        }
        100% {
          opacity: 1;
          transform: scale(1) translateY(0);
        }
      }

      /* Ball Color Classes */
      .ball.yellow {
        background: radial-gradient(
          circle at 30% 30%,
          #ffd94d,
          var(--color-1-10)
        );
      }
      .ball.blue {
        background: radial-gradient(
          circle at 30% 30%,
          #8ed8f7,
          var(--color-11-20)
        );
      }
      .ball.red {
        background: radial-gradient(
          circle at 30% 30%,
          #ff9e9e,
          var(--color-21-30)
        );
      }
      .ball.gray {
        background: radial-gradient(
          circle at 30% 30%,
          #cccccc,
          var(--color-31-40)
        );
      }
      .ball.green {
        background: radial-gradient(
          circle at 30% 30%,
          #c8e66e,
          var(--color-41-45)
        );
      }

      .draw-btn {
        background-color: var(--primary-color);
        color: white;
        border: none;
        padding: 16px 40px;
        font-size: 1.2rem;
        font-weight: 600;
        border-radius: 12px;
        cursor: pointer;
        transition: all 0.2s;
        box-shadow: 0 4px 15px rgba(74, 144, 226, 0.3);
        margin-bottom: 30px;
      }

      .draw-btn:hover {
        background-color: var(--primary-hover);
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(74, 144, 226, 0.4);
      }

      .draw-btn:active {
        transform: translateY(0);
      }

      .history-section {
        border-top: 1px solid var(--border-color);
        padding-top: 20px;
        transition: border-color 0.3s ease;
      }

      .history-title {
        font-size: 0.9rem;
        color: var(--subtitle-color);
        margin-bottom: 15px;
        transition: color 0.3s ease;
      }

      .history-list {
        list-style: none;
        max-height: 200px;
        overflow-y: auto;
      }

      .history-item {
        display: flex;
        justify-content: center;
        gap: 5px;
        margin-bottom: 8px;
        animation: fadeIn 0.3s ease-out;
      }

      .history-ball {
        width: 24px;
        height: 24px;
        border-radius: 50%;
        font-size: 0.7rem;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-weight: bold;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(-5px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Mobile Adjustments */
      @media (max-width: 400px) {
        .ball {
          width: 40px;
          height: 40px;
          font-size: 1rem;
        }
        .container {
          padding: 30px 15px;
        }
        .theme-toggle-btn {
          top: 15px;
          right: 15px;
          font-size: 1.2rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <button id="themeToggle" class="theme-toggle-btn" aria-label="테마 전환">
        🌙
      </button>

      <h1>🍀 로또 번호 추천</h1>

      <div id="ballContainer" class="ball-container">
        <!-- Balls will appear here -->
      </div>

      <button id="drawBtn" class="draw-btn">번호 생성하기</button>

      <div class="history-section">
        <p class="history-title">최근 추천 내역</p>
        <div id="historyList" class="history-list">
          <!-- History items will appear here -->
        </div>
      </div>
    </div>

    <script>
      const ballContainer = document.getElementById('ballContainer');
      const drawBtn = document.getElementById('drawBtn');
      const historyList = document.getElementById('historyList');
      const themeToggle = document.getElementById('themeToggle');

      // Theme toggle initialization
      const savedTheme = localStorage.getItem('theme');
      const prefersDark = window.matchMedia(
        '(prefers-color-scheme: dark)',
      ).matches;

      if (savedTheme === 'dark' || (!savedTheme && prefersDark)) {
        document.body.classList.add('dark-mode');
        themeToggle.textContent = '☀️';
        themeToggle.setAttribute('aria-label', '라이트 모드로 전환');
      } else {
        document.body.classList.remove('dark-mode');
        themeToggle.textContent = '🌙';
        themeToggle.setAttribute('aria-label', '다크 모드로 전환');
      }

      themeToggle.addEventListener('click', () => {
        const isDark = document.body.classList.toggle('dark-mode');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
        themeToggle.textContent = isDark ? '☀️' : '🌙';
        themeToggle.setAttribute(
          'aria-label',
          isDark ? '라이트 모드로 전환' : '다크 모드로 전환',
        );
      });

      function getBallColorClass(num) {
        if (num <= 10) return 'yellow';
        if (num <= 20) return 'blue';
        if (num <= 30) return 'red';
        if (num <= 40) return 'gray';
        return 'green';
      }

      function generateLottoNumbers() {
        const numbers = [];
        while (numbers.length < 6) {
          const r = Math.floor(Math.random() * 45) + 1;
          if (numbers.indexOf(r) === -1) numbers.push(r);
        }
        return numbers.sort((a, b) => a - b);
      }

      function createBall(num, index) {
        const ball = document.createElement('div');
        ball.className = `ball ${getBallColorClass(num)}`;
        ball.textContent = num;

        ballContainer.appendChild(ball);

        // Staggered animation
        setTimeout(() => {
          ball.classList.add('show');
        }, index * 150);
      }

      function addToHistory(numbers) {
        const item = document.createElement('div');
        item.className = 'history-item';

        numbers.forEach((num) => {
          const hBall = document.createElement('div');
          hBall.className = `history-ball ${getBallColorClass(num)}`;
          hBall.style.backgroundColor = `var(--color-${getBallColorClass(num).replace('yellow', '1-10').replace('blue', '11-20').replace('red', '21-30').replace('gray', '31-40').replace('green', '41-45')})`;

          // Manually apply background color because CSS variables in nested classes can be tricky
          const colorMap = {
            yellow: '#fbc400',
            blue: '#69c8f2',
            red: '#ff7272',
            gray: '#aaaaaa',
            green: '#b0d840',
          };
          hBall.style.backgroundColor = colorMap[getBallColorClass(num)];

          hBall.textContent = num;
          item.appendChild(hBall);
        });

        if (historyList.firstChild) {
          historyList.insertBefore(item, historyList.firstChild);
        } else {
          historyList.appendChild(item);
        }

        // Keep only last 10
        while (historyList.children.length > 10) {
          historyList.removeChild(historyList.lastChild);
        }
      }

      drawBtn.addEventListener('click', () => {
        // Disable button during animation
        drawBtn.disabled = true;
        drawBtn.textContent = '추첨 중...';

        // Clear previous balls
        ballContainer.innerHTML = '';

        const numbers = generateLottoNumbers();

        numbers.forEach((num, index) => {
          createBall(num, index);
        });

        // Re-enable button after all balls shown
        setTimeout(
          () => {
            drawBtn.disabled = false;
            drawBtn.textContent = '번호 생성하기';
            addToHistory(numbers);
          },
          6 * 150 + 500,
        );
      });

      // Initial draw
      // window.onload = () => drawBtn.click();
    </script>
  </body>
</html>
